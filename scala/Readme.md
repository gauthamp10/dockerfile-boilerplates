# Scala Boilerplate Docker

Build,compile and run Scala via docker

```
 docker build -t scala .

 docker run --rm scala
```
*Note: Scala build takes quite some time because scala by default requires java*

Build Size: 562MB